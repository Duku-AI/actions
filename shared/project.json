{
  "name": "duku-actions-shared",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "actions-repo/shared/src",
  "projectType": "library",
  "tags": ["type:github-action-lib", "scope:integrations"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run lint --prefix actions-repo/shared"
      },
      "cache": true,
      "dependsOn": [],
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "{projectRoot}/tsconfig.json",
        "{projectRoot}/package.json",
        "{projectRoot}/package-lock.json",
        "sharedGlobals"
      ],
      "outputs": []
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm test --prefix actions-repo/shared"
      },
      "cache": true,
      "dependsOn": ["lint"],
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "{projectRoot}/tsconfig.json",
        "{projectRoot}/package.json",
        "{projectRoot}/package-lock.json",
        "{projectRoot}/vitest.config.ts",
        "sharedGlobals"
      ],
      "outputs": []
    },
    "docker-build": {
      "options": { "command": "echo 'No docker build for github action lib'" },
      "dependsOn": [],
      "outputs": [],
      "cache": false
    },
    "docker-push": {
      "options": { "command": "echo 'No docker push for github action lib'" },
      "dependsOn": [],
      "outputs": [],
      "cache": false
    }
  }
}
